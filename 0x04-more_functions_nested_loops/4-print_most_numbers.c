#include "main.h"

/**
 * print_most_numbers - print digit (0 - 9).
 *
 * Return: Always 0.
*/

void print_most_numbers(void)
{
	int x;

	for (x = 48; x < 58; x++)
	{
		if (x != 50)
	{
		if (x != 52)
		{
			_putchar(c);
		}
	}
	}
	_putchar('\n');
}
